server:
  port: 8082

spring:
  application:
    name: payment-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
endpoint:
  prometheus:
    enabled: true
# Zipkin config
management.tracing.sampling.probability: 1.0
